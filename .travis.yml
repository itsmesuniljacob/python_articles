language: shell
sudo: true
addons:
    apt:
        packages:
            - aspell
            - aspell-en
            - jq
            - linkchecker
script:
    - shellcheck -x main.sh
    - bash main.sh
